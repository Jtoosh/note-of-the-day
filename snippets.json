[
    {
        "__type__": "Snippet",
        "text": "Here is a list of the ones that we used:\n`git remote -v` : shows the remote branches the local repo is connected to\n`git remote add <name> <url>`: add a remote branch with the given name\n`git fetch <name>` : fetch updates for a given remote branch\n`git log <remote/branch>` : view the git log for a given remote branch's branch\n`--abort` flag on `git merge`: undo a merge\n`-allow-unrelated-histories <remote/branch> s ours` flag on `git merge`: will only merge commit histories, not file changes\n`--oneline --graph` flag on `git log`: will print prettier git log that takes up less space\n`git tag <TAG-NAME>` : will apply a tag to the current or specified commit\n`--tags` flag on `git push`: will include tags in the commit.",
        "header": null,
        "file": "cs240_software_construction.md",
        "last_seen": null
    },
    {
        "__type__": "Snippet",
        "text": "Nav bar\nTitle\nPromo/headliner\nFlashcards page\nFlashcard set selection (`div`, `p`)\nIndividual cards, front and back ( series of `div`s)\nbuttons to edit/save cards (`button`)\nbuttons to move between cards (`button`)\nback button to exit set (`button`)\nCreate set button (`button`)\nQuiz page\nQuiz question (div, p)\nQuiz help button (button)\nFlag question button (button)\nbutton to move between questions (button)\nback button to exit quiz (button)\nQuiz selection (div)\nEdit quiz button (button)\nEdit quiz page\nSave quiz button (button)\nCreate quiz button (button)\nAccount page\nLogin button/icon\nAccount info fields\nlog out button\nedit account button\nsave account button\nstudy history/stats\nAbout page\npicture of me?",
        "header": null,
        "file": "cs260_web_programming.md",
        "last_seen": null
    },
    {
        "__type__": "Snippet",
        "text": "Must have a $ before it to signify immediate value\n`rmmovq` : move from register to memory\n`mrmovq` : move from memory to register\n`cmov__` : conditional moves (replace `__` with one of the conditional instruction suffixes shown below)\nALU Operations to register values:\n`addq` : addition\n`subq`: subtraction\n`andq` : And\n`xorq` : XOR\n`jmp` : Unconditional jump\n`j__` : Conditional jumps (replace `__` with one of the conditional instruction suffixes shown below)\n`pushq`: push onto stack register\n`popq` : pop from stack register\nFunctions:\n`call` : call a function\none other\n_Memory-to memory movement can't be done in a single instruction_.",
        "header": null,
        "file": "cs224_computer_systems.md",
        "last_seen": null
    },
    {
        "__type__": "Snippet",
        "text": "Commands to remember:\nTo instantiate the virutal environment: `python3 -m venv .venv` in the root directory\nTo activate the virtual environment: `source .venv/bin/activate`\nTo deactivate the virtual environment: `deactivate`\nUse pip to install needed packages\nTo run the MyPy checks: `mypy --strict <filename>`\nTo run a pre-commit check: `pre-commit run --all-files`\nTo run the tests: `pytest`\nTo run a certain pytest: `pytest <testname>`\nTo run a certain test in a certain file: `pytest <filename>::<testname>`\nTo get a list of the known tests: `pytest --collect-only`\n*Test driven development* (TDD) is the process of first writing a test for a new behavior in the code, running the test to see it fail (for whatever reason including missing definitions), and then writing code to make the test pass.",
        "header": null,
        "file": "cs236_discrete_structures.md",
        "last_seen": null
    }
]